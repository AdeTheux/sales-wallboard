<p id="notice"><%= notice %></p>

<h1>Wins</h1>

<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Agents</th>
      <th>Mrr</th>
      <th>Reps</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wins.each do |win| %>
      <tr>
        <td><%= win.company %></td>
        <td><%= win.agents %></td>
        <td><%= win.mrr %></td>
        <td><%= win.reps %></td>
        <td><%= link_to 'Show', win %></td>
        <td><%= link_to 'Edit', edit_win_path(win) %></td>
        <td><%= link_to 'Destroy', win, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Win', new_win_path %>
